# maxleimkuehler.de

One-page site for Max Leimkühler Consulting, served by GitHub Pages.
Static HTML, CSS and JavaScript, no build step. Fonts are self-hosted (SIL OFL).

Run the tests with Node 22 or newer:

    node --test test/*.test.js
